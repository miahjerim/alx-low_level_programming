#include "main.h"
/**
 * positive_or_negative - This function checks for negative or positive numbers
 *
 * @i: Is the value to be checked
 *
 * Return: always 0 on success
 */

void positive_or_negative(int i);

{
	if (i < 0)
		printf("%d is negative", i)
	else
		printf("%d is positive", i)
}
