#include <stdio.h>
/**
 * main -this prints the content followed by a new line
 *
 * Return: always is 0
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
